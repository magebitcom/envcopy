- name: Download mysqldump to project dir
  shell: "rsync -r -v --progress  -e 'ssh -i ~/.ssh/id_rsa' {{ aws_username }}@{{ project }}.stgin.com:/tmp/web.sql ./ > /dev/tty"
  args:
    chdir: "/var/www/{{ project }}/sql/"
